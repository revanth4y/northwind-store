/**
 * useOrderVideoPage — Stream Video SDK removed.
 * OrderVideoPage now simply redirects to /meet so this hook is unused.
 * Kept as an empty stub to prevent broken imports.
 */
export default function useOrderVideoPage() {
  return {};
}
